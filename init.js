window.onload = () => {
    // debugger;
    stringSorter.renderActiveList();

    // log initial state
    log.push({
        process: 'initialization',
    })
    // console.log('initialization done')
    return true;
}
